# DATABASE HEALTH REPORT

**Timestamp:** 2026-06-19 12:27:15 UTC  
**Database:** PostgreSQL (Neon)  
**Environment:** CollegeKhoj Admin System  
**Tool:** `run_diagnostics.py`

---

## 1. Table Status

| Table | Status | Row Count |
|-------|--------|-----------|
| `cutoffs` | ✅ Exists | 0 |
| `admission_types` | ✅ Exists | 3 |
| `academic_years` | ✅ Exists | 4 |
| `cap_rounds` | ✅ Exists | 3 |
| `colleges` | ✅ Exists | 12 |
| `branches` | ✅ Exists | 0 |
| `upload_jobs` | ✅ Exists | 1 |
| `audit_logs` | ✅ Exists | 6 |
| `backup_history` | ✅ Exists | 0 |

**Result:** All 9 required tables present. No missing tables.

---

## 2. Cutoff Table Column Validation

**15 columns found** — matches the model definition exactly:

```
id, admission_type_id, college_id, branch_id, academic_year_id,
cap_round_id, category, seat_type, gender, minority_status,
cutoff_percentile, cutoff_rank, source_pdf, upload_job_id, created_at
```

**Result:** ✅ All 15 required columns present. No missing columns.

---

## 3. Upload Job Status

| ID | Status | Error Message |
|----|--------|---------------|
| 1 | FAILED | Job aborted - cutoffs table was missing, database schema repaired |

### ⚠ Failed Job Analysis

**Job #1** (`DSE_CAP1_CutOff_2025_26-1-10.pdf`)

- **Error:** "Job aborted - cutoffs table was missing, database schema repaired"
- **Root Cause:** This upload job was created before the new admin v2 schema migration completed. When the background worker tried to process the PDF, the `cutoffs` table (one of the new v2 tables) did not exist yet. The upload worker aborted the job and the schema was subsequently repaired by `ensure_schema()` on the next app restart.
- **Impact:** No cutoff data was imported from this PDF.
- **Fix:** Re-upload the PDF file `DSE_CAP1_CutOff_2025_26-1-10.pdf` through the admin import interface now that the `cutoffs` table exists. Alternatively, create a retry mechanism for failed jobs.

---

## 4. ORM Validation

| Check | Result |
|-------|--------|
| `Cutoff.query.count()` | 0 |
| Raw SQL `COUNT(*)` | 0 |
| Counts Match | ✅ Yes |

**Result:** ✅ ORM model maps correctly to the `cutoffs` table. No model/table mismatch.

---

## 5. Session & Connection Health

| Check | Status |
|-------|--------|
| Database Dialect | PostgreSQL |
| Flask App Context | ✅ Active |
| DB Connection | ✅ Connected |
| SQLAlchemy Engine | ✅ Healthy |

**Result:** ✅ All systems operational. Database session is healthy.

---

## 6. Additional Observations

- **`branches` table has 0 rows:** Branch data needs to be seeded/imported before cutoff data can be fully utilized (it's a foreign key dependency).
- **`cutoffs` table has 0 rows:** The only upload job failed. No cutoff data exists yet.
- **`backup_history` has 0 rows:** No database backups have been performed yet.

---

## 7. Recommendations

1. **Re-upload the failed PDF** (`DSE_CAP1_CutOff_2025_26-1-10.pdf`) via the admin import page now that the `cutoffs` table exists.
2. **Seed branch data** if branch records are needed — run the seed script or import branch reference data.
3. **Consider a retry mechanism** in `admin/background_worker.py` to automatically retry FAILED upload jobs after schema repairs.
4. **Perform a database backup** via `/admin/system/backups/create` to establish the first backup baseline.

---

*Report generated by `run_diagnostics.py` — read-only diagnostic, no data was modified.*